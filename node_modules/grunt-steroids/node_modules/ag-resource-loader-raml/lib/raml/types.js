(function() {
  var traverseByPath, types,
    __slice = [].slice;

  types = require('ag-types');

  traverseByPath = function(path, leaf) {
    var head, tail, _ref;
    if (leaf == null) {
      leaf = types.Any;
    }
    if (path) {
      _ref = path.split('.'), head = _ref[0], tail = 2 <= _ref.length ? __slice.call(_ref, 1) : [];
      if (tail.length) {
        return types.Property(head, traverseByPath(tail.join('.'), leaf));
      } else {
        return types.Property(head, leaf);
      }
    } else {
      return leaf;
    }
  };

  module.exports = {
    Any: types.Any,
    TraverseByPath: traverseByPath,
    FromProperty: types.projections.Property,
    Try: types.Try
  };

}).call(this);
